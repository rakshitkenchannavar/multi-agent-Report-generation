# =============================================================================
# AI Report Generation System — Python dependencies
# Provider: Google AI Studio (Gemini)
# Architecture: Microsoft Agent Framework (MAF) pattern
# All versions pinned to avoid pip resolution-too-deep on Render
# =============================================================================

# --- App / API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Settings / validation ---
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1

# --- LLM: Google Gemini ---
google-genai==1.2.0

# --- Database & Authentication ---
sqlmodel==0.0.22
passlib==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0

# --- Research tools (Tavily web search) ---
tavily-python==0.5.0
requests==2.32.3

# --- Report export (PDF + DOCX) ---
reportlab==4.2.5
python-docx==1.1.2

# =============================================================================
# Optional providers — uncomment only when switching
# =============================================================================
# Azure OpenAI:
# openai==1.59.6
#
# NOTE: agent-framework packages removed.
# The workflow follows MAF architecture (executors, structured contracts,
# conditional routing), but the runtime LLM client uses google-genai directly
# because agent-framework-google is only published as 0.0.0a1 alpha.