# =============================================================================
# AI Report Generation System — Python dependencies
# Provider default: Google AI Studio (Gemini)
# Framework: Microsoft Agent Framework (MAF)
# =============================================================================

# --- Core framework (PyPI-compatible pins) ---
agent-framework>=1.13.0
agent-framework-core>=1.13.0

# Google / Gemini integration (currently published as early alpha on PyPI)
agent-framework-google==0.0.0a1
google-genai>=1.0.0

# --- Azure OpenAI (optional — enable later via LLM_PROVIDER=azure_openai) ---
# Uncomment when you switch to Azure:
# agent-framework-azure-ai>=1.13.0
# openai>=1.40.0
# azure-identity>=1.17.0

# --- OpenAI non-Azure (optional) ---
# agent-framework-openai>=1.13.0
# openai>=1.40.0

# --- App / API ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# --- Settings / validation ---
pydantic>=2.8.0
pydantic-settings>=2.4.0
python-dotenv>=1.0.1

# --- HTTP (SerpAPI / general) ---
requests>=2.32.0
httpx>=0.27.0

# --- Research tools (optional — only if ENABLE_WEB_SEARCH=true) ---
tavily-python>=0.5.0

# --- Report export (Markdown built-in; PDF + DOCX below) ---
reportlab>=4.2.0
python-docx>=1.1.2

# --- Dev / quality (optional) ---
# pytest>=8.0.0
# pytest-asyncio>=0.23.0
# ruff>=0.6.0